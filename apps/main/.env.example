# ===========================================
# Google Maps Configuration - Environment Variables
# ===========================================
# 
# This file contains example environment variables for Google Maps integration.
# Copy this file to .env.local and update the values for your local development.
# 
# IMPORTANT SECURITY NOTES:
# - Never commit actual API keys to version control
# - Use .env.local for local development (automatically git-ignored)
# - All variables with VITE_ prefix are exposed to the client-side code
# - Phase 1 uses Embed API (free, no API key required)
# - Phase 2 will require Google Maps JavaScript API key

# ===========================================
# Google Maps Embed API Configuration (Phase 1)
# ===========================================

# Google Maps Embed API base URL for place embedding
# This is used for embedding maps without requiring an API key
VITE_GOOGLE_MAPS_EMBED_BASE_URL=https://www.google.com/maps/embed/v1/place

# Business address for map location
# Format: Street Address, City, State ZIP
VITE_BUSINESS_ADDRESS=123 Harmony Lane, Roy, WA 98580

# Business name for map display
# This is used in the map embedding URL
VITE_BUSINESS_NAME=The Reiki Goddess Healing

# ===========================================
# Map Display Configuration
# ===========================================

# Default zoom level for maps (1-21, where 21 is most zoomed in)
# 15 is a good balance showing the business location with some context
VITE_MAP_DEFAULT_ZOOM=15

# Map center coordinates (latitude, longitude)
# These coordinates are for Roy, WA area
VITE_MAP_CENTER_LAT=46.9899
VITE_MAP_CENTER_LNG=-122.5434

# ===========================================
# Phase 2 Configuration (Future Use)
# ===========================================
# The following variables are prepared for Phase 2 implementation
# when we upgrade to Google Maps JavaScript API for advanced features

# Google Maps JavaScript API Key (Phase 2 - Currently not used)
# Get your API key from: https://console.developers.google.com/
# Enable: Maps JavaScript API, Places API (if needed)
# VITE_GOOGLE_MAPS_API_KEY=your-api-key-here

# Map style configuration (Phase 2)
# Options: roadmap, satellite, hybrid, terrain
# VITE_MAP_TYPE=roadmap

# Enable/disable map controls (Phase 2)
# VITE_MAP_ENABLE_ZOOM_CONTROL=true
# VITE_MAP_ENABLE_STREET_VIEW=false
# VITE_MAP_ENABLE_FULLSCREEN=true

# Custom map styling (Phase 2)
# JSON string for custom map styles
# VITE_MAP_CUSTOM_STYLES=[]
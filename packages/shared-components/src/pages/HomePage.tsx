import React from "react";
import { Homepage } from "../Homepage/Homepage";

/**
 * HomePage - Root route page component
 * Uses the complete Homepage component with all sections
 */
export const HomePage: React.FC = () => {
  return <Homepage />;
};
